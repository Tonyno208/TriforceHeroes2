<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Título del diálogo -->
    <TextView
        android:id="@+id/tvDialogTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/select_logo_title"
        android:textSize="20sp"
        android:textStyle="bold"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="16dp"/>

    <!-- Contenedor de logos en un layout horizontal -->
    <LinearLayout
        android:id="@+id/llLogos"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp">

        <!-- Logo Dark -->
        <ImageView
            android:id="@+id/ivLogoDark"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:src="@drawable/imagen1"
            android:contentDescription="@string/logo_dark"
            android:adjustViewBounds="true"
            android:scaleType="fitCenter"
            android:padding="8dp"
            android:clickable="true"
            android:focusable="true" />

        <!-- Logo Light -->
        <ImageView
            android:id="@+id/ivLogoLight"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:src="@drawable/imagen2"
            android:contentDescription="@string/logo_light"
            android:adjustViewBounds="true"
            android:scaleType="fitCenter"
            android:padding="8dp"
            android:clickable="true"
            android:focusable="true" />

        <!-- Logo Default -->
        <ImageView
            android:id="@+id/ivLogoDefault"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:src="@drawable/imagen3"
            android:contentDescription="@string/logo_default"
            android:adjustViewBounds="true"
            android:scaleType="fitCenter"
            android:padding="8dp"
            android:clickable="true"
            android:focusable="true" />
    </LinearLayout>
</LinearLayout>
